// app/kiosk.tsx
// Top-level kiosk route — accessible without login (no auth group, no admin group).
// The actual screen lives in src/screens/KioskScreen.tsx to avoid duplication.
export { default } from '../src/screens/KioskScreen';
